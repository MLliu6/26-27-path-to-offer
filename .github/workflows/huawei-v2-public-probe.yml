name: huawei-v2-public-probe

on:
  pull_request:
    paths:
      - 'tests/huawei_v2_public_probe.py'
      - '.github/workflows/huawei-v2-public-probe.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Install Playwright Python package
        run: python -m pip install --disable-pip-version-check playwright
      - name: Observe Huawei public recruiting page
        run: python tests/huawei_v2_public_probe.py
